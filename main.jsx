import React from 'react'
import ReactDOM from 'react-dom/client'
import SmartModelInterface from './SmartModelInterface'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <SmartModelInterface />
  </React.StrictMode>
)